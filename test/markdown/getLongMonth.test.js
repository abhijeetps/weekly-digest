// var getLongDate = require('./../../src/markdown/getLongMonth')

test('that we can run tests', () => {
  // your real tests go here
  expect(1 + 2 + 3).toBe(6)
})

// test('that passing 0 returns january', () => {
//   expect(getLongDate(0)).toBe('January')
// })
